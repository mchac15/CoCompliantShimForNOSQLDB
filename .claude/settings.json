{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_input.file_path // empty\" | { read -r f; case \"$f\" in */.claude/worktrees/*) exit 0 ;; */pseudo.txt|pseudo.txt) echo \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PostToolUse\\\",\\\"additionalContext\\\":\\\"pseudo.txt was just modified. Invoke the pseudocode-tex-sync skill now to update main.tex so it reflects this pseudocode change, staying Overleaf-compatible.\\\"}}\" ;; esac; }"
          }
        ]
      }
    ]
  }
}
